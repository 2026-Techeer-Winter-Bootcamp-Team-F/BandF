global:
  scrape_interval: 15s #메트릭 수집 인터벌
  evaluation_interval: 15s #메트릭 평가 인터벌

scrape_configs:
  - job_name: 'prometheus' #프로메테우스 자체 모니터링
    static_configs:
      - targets: ['localhost:9090'] #자기 자신은 localhost로 접근
  # traefik 서비스가 없으므로 주석 처리
  # - job_name: 'traefik'
  #   static_configs:
  #     - targets: ['traefik:8091']
  
  # 백엔드 메트릭 수집이 필요하면 아래 주석 해제 (django-prometheus 설치 필요)
  # - job_name: 'backend'
  #   static_configs:
  #     - targets: ['backend:8000']

